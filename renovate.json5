{
  $schema: 'https://docs.renovatebot.com/renovate-schema.json',
  repositories: [
    'PowerOfCreation/keycloak-dhi',
  ],
  minimumReleaseAge: '48 hours',
  prHourlyLimit: 10,
  packageRules: [
    {
      matchUpdateTypes: [
        'pinDigest',
        'digest',
      ],
      minimumReleaseAge: null,
    },
    {
      matchDatasources: [
        'docker',
      ],
      minimumReleaseAge: null,
      matchPackageNames: [
        '/^ghcr\\.io//',
      ],
    },
    {
      // Builder- (-dev) und Runtime-Tag müssen synchron bleiben, sonst baut
      // die Builder-Stage eine andere Keycloak-Version als die Runtime erwartet.
      matchPackageNames: [
        'dhi.io/keycloak',
      ],
      groupName: 'keycloak (dhi builder + runtime)',
    },
  ],
  extends: [
    'config:best-practices',
    'helpers:pinGitHubActionDigestsToSemver',
  ],
  customManagers: [
    {
      customType: 'regex',
      managerFilePatterns: [
        '/^.github/workflows/renovate.yml$/',
      ],
      matchStrings: [
        'renovate-version: (?<currentValue>[\\d.]+)@(?<currentDigest>sha256:[a-f0-9]+)',
      ],
      depNameTemplate: 'ghcr.io/renovatebot/renovate',
      datasourceTemplate: 'docker',
      versioningTemplate: 'semver',
    },
  ],
}