{
  $schema: 'https://docs.renovatebot.com/renovate-schema.json',
  minimumReleaseAge: '48 hours',
  prHourlyLimit: 10,
  packageRules: [
    {
      matchUpdateTypes: [
        'pinDigest',
        'digest',
      ],
      minimumReleaseAge: null,
    },
    {
      matchDatasources: [
        'docker',
      ],
      minimumReleaseAge: null,
      matchPackageNames: [
        '/^ghcr\\.io//',
      ],
    },
    {
      // Builder (-dev) and runtime tag must stay in sync, otherwise the
      // builder stage would build a different Keycloak version than the runtime expects.
      matchPackageNames: [
        'dhi.io/keycloak',
      ],
      groupName: 'keycloak (dhi builder + runtime)',
    },
  ],
  extends: [
    'config:best-practices',
    'helpers:pinGitHubActionDigestsToSemver',
  ],
  customManagers: [
    {
      customType: 'regex',
      managerFilePatterns: [
        '/^.github/workflows/renovate.yml$/',
      ],
      matchStrings: [
        'renovate-version: (?<currentValue>[\\d.]+)@(?<currentDigest>sha256:[a-f0-9]+)',
      ],
      depNameTemplate: 'ghcr.io/renovatebot/renovate',
      datasourceTemplate: 'docker',
      versioningTemplate: 'semver',
    },
  ],
}